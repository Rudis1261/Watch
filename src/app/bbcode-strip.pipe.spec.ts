import { BbcodeStripPipe } from './bbcode-strip.pipe';

describe('BbcodeStripPipe', () => {
  it('create an instance', () => {
    const pipe = new BbcodeStripPipe();
    expect(pipe).toBeTruthy();
  });
});
